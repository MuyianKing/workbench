{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的默认能力：核心 API（窗口 / 事件 / webview）加文件对话框与用系统程序打开。标题栏自绘后要按 data-tauri-drag-region 拖动窗口，必须额外放行 core:window:allow-start-dragging —— core:default 只含 allow-internal-toggle-maximize，拖动命令被 ACL 拦下后是静默失效（按下去毫无反应，也不报错）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "opener:default"
  ]
}
