/target

# 验证用的临时 target 目录（`cargo test` 卡在链接时的绕法，见 docs/dev-notes/browser-preview-verification.md）。
# 里面是构建产物，不进版本库；一个 build-script-build.exe 曾经就这么被提交过。
/target-verify
/target-cfgtest

# 账号登录用的 OAuth 应用凭据（client_id / client_secret）。
# **绝不能入库**：这是别人能把请求冒充成本应用的凭据。
# 模板见 oauth.example.json，构建时由 build.rs 打进二进制。
/oauth.local.json
